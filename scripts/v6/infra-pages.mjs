import { esc,layout,list } from './common.mjs'
const TECH=[
 ['Tailscale','private mesh / access','Conectar máquinas em redes diferentes sem publicar painéis, bancos e serviços administrativos diretamente na internet.','Connect machines across different networks without publishing administrative services to the public internet.'],
 ['Cloudflare Tunnel','edge / routes / access','Publicar apenas serviços que precisam de nomes e rotas públicas, mantendo a origem atrás da borda.','Publish only services that need public routes while keeping origins behind the edge.'],
 ['Docker','containers / compose','Fazer serviços coexistirem, migrarem e voltarem por definições reproduzíveis.','Keep services isolated, movable and recoverable through reproducible definitions.'],
 ['Restic + SFTP','backup / verify / restore','Manter snapshots versionados, verificar integridade e tratar restore como critério de aceite.','Keep versioned snapshots, verify integrity and treat restore as the acceptance criterion.'],
 ['Nginx / Caddy','reverse proxy / TLS','Usar controle explícito onde necessário e simplificar TLS/reverse proxy em serviços menores.','Use explicit control where needed and simpler TLS/reverse proxying for smaller services.'],
 ['Uptime Kuma','monitoring / checks','Transformar “acho que está no ar” em checagens, histórico e sinais observáveis.','Turn “it should be online” into observable checks and history.'],
 ['Pterodactyl','games / operations','Separar o ciclo de vida dos servidores de jogos da infraestrutura web geral.','Separate game-server lifecycle from the general web stack.'],
 ['SSH','ops / recovery','Manter um caminho operacional de baixo nível para diagnóstico, manutenção e recuperação.','Keep a low-level path for diagnosis, maintenance and recovery.'],
]
const nodeCard=(name,role,href,status,extra='')=>`<a class="infra-node-card" href="${href}"><div><span class="status-dot"></span><strong>${esc(name)}</strong></div><p>${esc(role)}</p><small>${esc(status)}${extra?` · ${esc(extra)}`:''}</small></a>`
export function infrastructurePage(profile,links,infra,lang='pt'){const en=lang==='en',path=en?'/en/infrastructure/':'/infraestrutura/',by=Object.fromEntries(infra.servers.map(s=>[s.slug,s]));const body=`<section class="page-hero"><div class="shell"><p class="eyebrow">INFRASTRUCTURE</p><h1>${en?'Infrastructure as <span>part of the product.</span>':'Infraestrutura como <span>parte do produto.</span>'}</h1><p class="lead">${en?'A self-hosted laboratory for deployment, networking, containers, backup and recovery. The public view shows roles without exposing sensitive topology.':'Um laboratório self-hosted para deploy, redes, contêineres, backup e recuperação. A visão pública mostra papéis sem expor topologia sensível.'}</p></div></section><section class="section"><div class="shell"><div class="infra-topology"><svg viewBox="0 0 1000 560"><path d="M500 105 C365 145 270 238 190 398"/><path d="M500 105 C500 210 500 285 500 440"/><path d="M500 105 C635 145 730 238 810 398"/><path d="M190 398 C330 468 670 468 810 398"/></svg><div class="topology-node edge"><a href="/borda/"><strong>${en?'Edge':'Borda'}</strong><span>routes · protection</span></a></div><div class="topology-node hefesto"><a href="/hefesto/"><strong>Hefesto</strong><span>apps · containers</span></a></div><div class="topology-node goiatuba"><a href="/goiatuba/"><strong>Goiatuba</strong><span>${en?'name under review':'nome em revisão'}</span></a></div><div class="topology-node poseidon"><a href="/poseidon/"><strong>Poseidon</strong><span>services · games</span></a></div></div></div></section><section class="section"><div class="shell"><div class="section-heading"><div><p class="eyebrow">${en?'NODES':'NÓS'}</p><h2>${en?'Every main node has a public page.':'Cada nó principal tem uma página pública.'}</h2></div></div><div class="infra-node-grid">${nodeCard(en?'Edge':'Borda',en?'Public routes and origin boundaries.':'Rotas públicas e limites de origem.','/borda/',en?'Architectural role':'Papel arquitetural')}${nodeCard('Hefesto',by.hefesto?.role||'','/hefesto/',by.hefesto?.publicStatus||'')}${nodeCard('Goiatuba',by.goiatuba?.role||'','/goiatuba/',by.goiatuba?.publicStatus||'',en?'name under review':'nome em revisão')}${nodeCard('Poseidon',by.poseidon?.role||'','/poseidon/',by.poseidon?.publicStatus||'')}</div></div></section><section class="section"><div class="shell"><div class="section-heading"><div><p class="eyebrow">STACK</p><h2>${en?'Tools chosen for specific problems.':'Ferramentas escolhidas por problemas específicos.'}</h2></div><p>${en?'Not a generic cloud-stack list.':'Não é uma lista genérica de cloud.'}</p></div><div class="tech-matrix">${TECH.map(([n,c,pt,eng])=>`<article class="tech-card"><h3>${n}</h3><span>${c}</span><p>${esc(en?eng:pt)}</p></article>`).join('')}</div></div></section>`;return layout({profile,links,title:en?`Infrastructure | ${profile.name}`:`Infraestrutura | ${profile.name}`,description:en?'Public sanitized infrastructure view.':'Visão pública e sanitizada do laboratório de infraestrutura.',path,body,lang})}
export function infraDetail(profile,links,server,type='server'){const edge=type==='edge',name=edge?'Borda':server.name,role=edge?'Camada pública de rotas, DNS, proteção e separação entre origem e superfície exposta.':server.role,platform=edge?'Cloudflare / edge routing':server.platform,caps=edge?['DNS','Cloudflare Tunnel','Rotas públicas','Controles de acesso','Separação de origem']:server.capabilities,boundary=edge?'A página descreve funções públicas sem expor tokens, regras privadas, endereços de origem ou painéis administrativos.':server.boundary,path=edge?'/borda/':`/${server.slug}/`;const extra=server?.slug==='goiatuba'?'<p class="callout">Nome do nó em revisão.</p>':'';const body=`<section class="page-hero"><div class="shell"><p class="eyebrow">INFRASTRUCTURE / NODE</p><h1>${esc(name)}</h1><p class="lead">${esc(role)}</p>${extra}</div></section><section class="section"><div class="shell about-grid"><article class="surface-card"><p class="eyebrow">PUBLIC PROFILE</p><h2>${esc(platform)}</h2>${list(caps||[])}</article><article class="surface-card"><p class="eyebrow">BOUNDARY</p><h2>Public ≠ admin</h2><p>${esc(boundary)}</p><a class="text-link" href="/infraestrutura/">← Voltar para infraestrutura</a></article></div></section>`;return layout({profile,links,title:`${name} | Infraestrutura Wícolly`,description:role,path,body})}
